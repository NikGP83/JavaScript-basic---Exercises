// Write a JavaScript program to concatenate two strings and return the result. 
// If the length of the strings are not same then remove the characters from the longer string.

const concatTwoStrgs = (strOne, strTwo) => {
    const result = (strOne.length === strTwo.length);
    return result
}
    // const strLTwo = strTwo.length;
//     if (strLOne > strLTwo || strLTwo > strLOne)
//      const result = (strOne.split(``));
     
//      return result
// } 

console.log(concatTwoStrgs(`Арбузный`, `Вишнёвоклёвый`));


//WIP !!!!!!